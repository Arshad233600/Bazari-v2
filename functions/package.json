{
  "name": "bazari-functions",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "algoliasearch": "^4.23.0",
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^4.8.0",
    "meilisearch": "^0.40.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.11.0"
  }
}